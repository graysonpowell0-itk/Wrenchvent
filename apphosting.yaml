runConfig:
  runCommand: npm start

buildConfig:
  runtime: nodejs20
  buildCommand: npm install && npm run build
  outputDirectory: build

env:
  - variable: NODE_ENV
    value: production
    availability:
      - BUILD
      - RUNTIME
